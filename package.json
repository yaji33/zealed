{
  "name": "zealed",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm --filter @zealed/web dev",
    "keeper": "pnpm --filter @zealed/contracts keeper:sepolia",
    "test": "turbo run test",
    "compile": "pnpm --filter @zealed/contracts compile"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "pnpm": {
    "overrides": {
      "@zama-fhe/relayer-sdk": "0.4.1"
    }
  }
}
